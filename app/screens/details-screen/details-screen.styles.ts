import { StyleSheet } from "react-native";

export const DetailStyles = StyleSheet.create({
    container: {
        flex: 1
    },
    FAB: {
        position: 'absolute',
        margin: 30,
        right: 0,
        bottom: 0,
    }
})